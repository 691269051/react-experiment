module.exports = {
    stories: ['../src/**/*.stories.[t|j]s[x]'],
    addons: [
        '@storybook/preset-create-react-app',
        '@storybook/addon-actions',
        '@storybook/addon-links',
    ],
}
